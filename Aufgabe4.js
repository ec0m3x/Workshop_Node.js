const axios = require('axios');

// Daten, die du senden möchtest (z.B. als JSON)
const postData = {
    title: 'Post Title',
    body: 'This is the body of the post',
    userId: 1
};

// URL des Zielservers
const url = 'https://jsonplaceholder.typicode.com/posts';

//GET-Anfrage 


// POST-Anfrage 
